const numero = 5

// na primeira condição verdadeira o javascript vai sair fora da estrutura de condição
if(numero >= 0 && numero <= 5) {
    console.log(`10 >= ${numero} >= 0`)
} else {
    console.log("condições não compridas")
}

console.log("independente do resultado da condição o código vai continuar")