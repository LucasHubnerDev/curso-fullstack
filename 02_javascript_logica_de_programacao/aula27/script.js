// Operação Ternária
// (condição) ? (valor para verdadeiro) : (valor para false)

let cor;

console.log(`${cor ? cor : "cor padrão"}`)

let statu;
const money = "3000"
const verificacao = money >= 3000 ? statu = "vip": statu = "normal"; 

console.log(statu)

let valor = 200;
const valorPadrao = valor || "100"

console.log(valorPadrao)